name: CI admin build

on:
  workflow_dispatch:
    inputs:
      build_version:
        type: string
        description: Build version
        required: true
      source_branch:
        type: string
        description: Admin source branch
        required: true
        default: main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID2 }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY2 }}
      AWS_REGION: us-west-2
      ENVIRONMENT: staging
      VITE_API_URL: https://api.totembound-test.net/v1
      VITE_VERSION: ${{ github.event.inputs.build_version }}
      # Optional external dashboard deep-links (blank = hidden in the UI).
      VITE_CLOUDWATCH_DASHBOARD_URL: ${{ secrets.ADMIN_CLOUDWATCH_DASHBOARD_URL_STAGING }}
      VITE_POSTHOG_DASHBOARD_URL: ${{ secrets.ADMIN_POSTHOG_DASHBOARD_URL_STAGING }}

    steps:
    # totem-admin is a PRIVATE repo — checkout needs the org PAT secret.
    - name: Checkout admin repo
      uses: actions/checkout@v5
      with:
        repository: totembound/totem-admin
        ref: ${{ github.event.inputs.source_branch }}
        token: ${{ secrets.PAT }}
        path: totem-admin

    - name: Setup Node
      uses: actions/setup-node@v5
      with:
        node-version: '22'

    - name: Install dependencies
      working-directory: ./totem-admin
      run: npm ci

    - name: Check types
      working-directory: ./totem-admin
      run: npm run check-types

    - name: Run ESLint
      working-directory: ./totem-admin
      run: npm run lint

    - name: Run tests
      working-directory: ./totem-admin
      run: npm run test:ci

    - name: Build
      working-directory: ./totem-admin
      run: npm run build

    - name: Zip release
      working-directory: ./totem-admin
      run: zip -r release-${{ github.event.inputs.build_version }}.zip ./build

    - name: Push to S3
      working-directory: ./totem-admin
      run: aws s3 sync . s3://totemboundci-releases/totem-admin --exclude "*" --include "release-${{ github.event.inputs.build_version }}.zip"
